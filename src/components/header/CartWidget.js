function CartWidget() {
  const cantidad = 0;

  return (
    <>
      <button>
        <span className="material-icons">shopping_cart</span>
        <span>{cantidad}</span>
      </button>
    </>
  );
}

export default CartWidget;
